flowchart LR
  subgraph agents["Your agents — unchanged workflow"]
    direction TB
    CC[Claude Code]:::agent
    CX[Codex]:::agent
    GM[Gemini CLI]:::agent
    OC[opencode]:::agent
  end

  subgraph surfaces["Artesian surfaces"]
    direction TB
    MCP[artesian-mcp]:::surface
    CLI["artesian-cli / artesiand"]:::surface
    TUI["gauge — TUI"]:::surface
  end

  CORE[artesian-core]:::core

  MEM["Aquifer — memory<br/><b>the flagship: own it, ~1k tokens/query</b>"]:::flagship

  subgraph stores["Memory stores — pluggable"]
    direction TB
    VS[("VectorStore<br/>Qdrant / sqlite-vec")]:::store
    FB[("Files — OKF bundle")]:::store
  end

  subgraph opt["Optional components — opt in as needed"]
    direction TB
    ORCH["Basin<br/>master / worker / judge"]:::cap
    TEAMS["Wellfield — agent teams"]:::cap
    TASKS["headrace — task tracking"]:::cap
    SBX["sandbox — Docker isolation"]:::cap
  end

  agents -->|MCP| MCP
  MCP --> CORE
  CLI --> CORE
  TUI --> CORE
  CORE ==> MEM
  MEM --> VS
  MEM --> FB
  CORE -.->|opt in| opt
  ORCH -.->|same store| MEM
  TEAMS -.->|same store| MEM

  classDef agent    fill:#dde0f0,stroke:#3b4a96,color:#1a1f4a;
  classDef surface  fill:#cdeef2,stroke:#0f9aa8,color:#073740;
  classDef core     fill:#fcecc9,stroke:#c98a1e,color:#3d2700;
  classDef flagship fill:#cfeae3,stroke:#1f8a70,color:#0b3020,stroke-width:2px;
  classDef cap      fill:#e7e9ec,stroke:#5b6770,color:#1a1f24;
  classDef store    fill:#d7ecf5,stroke:#1b6ca8,color:#082040;
